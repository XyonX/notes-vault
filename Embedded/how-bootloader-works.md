bootloader start at the powerr on 
it checks for avaible firmware in the flash memory if presdent it goes into running it
in this staget the bootloader goes to idle mode
later if we press hte  entering boootleade mode button(varied by board) it will male the bootloader from idle to runnign 

now in the bootlaodfer mode it listed from the uploading software in usb or usart input
