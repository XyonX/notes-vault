## find_max_average
- Sliding window for maximum average of k consecutive elements. Notes: O(n) time, handles edges like full array or negatives.

## longest_unique_subarray
- Sliding window for longest contiguous unique elements using last-index map. Notes: O(n) time, extensive tests pass.

