# Heaps Compact Version

## max_heap_implementation
- **Approach**: Custom max heap with insert and heapify-up. Uses vector storage, swaps to maintain parent >= children.
- **Time**: O(log n) insert
- **Space**: O(n)
- **Key**: Fundamental for priority queues; extend for delete-min (heapify-down).