# Heaps Discussion

## max_heap_implementation
This implementation covers the basics of building a max heap through insertions. Each insert adds to the end and bubbles up by swapping with parent if needed. Important for understanding how priority queues work internally. Next steps: Implement heapify-down for deletions and full heap construction in O(n).